import 'package:flutter/material.dart';

class LocalizacaoTab extends StatelessWidget {
  final PageController _pageController;

  LocalizacaoTab(this._pageController);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
